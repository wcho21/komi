#!/usr/bin/env bash
# install-git-hooks: for dev container to run in "postCreateCommand" field

echo "install-git-hooks: setting pre-commit hook..."
cp .hooks/pre-commit .git/hooks/pre-commit
echo "install-git-hooks: hook installed."